# المعاينة المحلية الثابتة

هذا المشروع Flutter. لتجنب بناء نسخة إنتاجية مع كل تعديل، شغّل خادم Flutter التطويري محليًا؛ سيبقى الرابط ثابتًا أثناء عملك.

## التشغيل

من داخل مجلد المشروع:

```bash
chmod +x tool/local_preview.sh
tool/local_preview.sh main 8080
```

ثم افتح الرابط التالي في Chrome:

```text
http://localhost:8080
```

اترك نافذة الطرفية مفتوحة. السكربت يفحص فرع `main` في GitHub كل 5 ثوانٍ، ويسحب آخر Commit تلقائيًا، ثم يعيد تشغيل خادم المعاينة على **نفس الرابط**. بعد ظهور رسالة `Preview updated` حدّث صفحة Chrome.

يمكن تغيير الفترة:

```bash
PREVIEW_PULL_INTERVAL=10 tool/local_preview.sh main 8080
```

## سير العمل اليومي

1. شغّل السكربت مرة واحدة.
2. افتح `http://localhost:8080` في Chrome.
3. عدّل الكود وارفعه إلى GitHub:

```bash
git add .
git commit -m "Update app"
git push origin main
```

4. انتظر تحديث المعاينة، ثم حدّث صفحة Chrome.
5. عند الانتهاء فقط نفّذ بناء الإصدار النهائي الذي تحتاجه، مثل:

```bash
flutter build apk --release
```

## ملاحظات مهمة

- هذا المسار لا ينفذ `flutter build web` عند كل تعديل.
- خادم التطوير قد يعيد ترجمة الملفات اللازمة داخليًا، وهذا أسرع ومختلف عن بناء نسخة إنتاجية.
- إذا كان لديك تعديل محلي غير مرفوع إلى GitHub، يتوقف السحب التلقائي لحمايته من الاستبدال.
- لإيقاف المعاينة اضغط `Ctrl+C` في نافذة السكربت.
- إذا كان المنفذ 8080 مستخدمًا، شغّل مثلًا `tool/local_preview.sh main 8081` وافتح `http://localhost:8081`.
